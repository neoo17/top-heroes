// Укажите URL вашего API (Render/Railway).
// GitHub Pages будет обращаться к этому адресу для запросов /api/*.
// Если оставить закомментированным — фронтенд попытается работать от того же домена (локальный режим).

window.API_BASE = 'https://top-heroes-1.onrender.com';
